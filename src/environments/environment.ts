// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  fireBaseConfig:{
    apiKey: "AIzaSyAYiml23SKnp0ZBq7nf5bQJ3wHQYZI0hm0",
    authDomain: "my-super-duper-project-ad2c0.firebaseapp.com",
    databaseURL: "https://my-super-duper-project-ad2c0.firebaseio.com",
    projectId: "my-super-duper-project-ad2c0",
    storageBucket: "my-super-duper-project-ad2c0.appspot.com",
    messagingSenderId: "428226547467"  
  }
}

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
